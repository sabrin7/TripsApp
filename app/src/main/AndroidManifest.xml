<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sabri.excursion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.LogsignActivity" />
        <activity android:name=".controller.SignActivity" />
        <activity android:name=".controller.LogActivity" />
        <activity android:name=".controller.AddtripActivity" />
        <activity android:name=".controller.AgencyspaceActivity" />
        <activity android:name=".controller.BookActivity" />
        <activity android:name=".controller.UserspaceActivity" />
        <activity android:name=".controller.ModifytripActivity" />
        <activity android:name=".controller.SuggestionActivity"></activity>
    </application>

</manifest>